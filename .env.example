# Base path for GitHub Pages (repo name, leave empty for root)
BASE_PATH=resume

# Name slug for filenames (generates: {name}_cv_{lang}.pdf)
CV_NAME=john-doe

# Supported languages (comma-separated, first is default at root /)
LANGUAGES=en,es
